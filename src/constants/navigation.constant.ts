export enum ROUTES {
  AUTH = "/auth",
  LOGIN = "/auth/login",
  SIGNUP = "/auth/signup",
  HOME = "/",
  FRIENDS = "/friends",
  CHATS = "/chats",
  CHAT = "/chat/:id",
  TODOS = "/todos",
  PROFILE = "/profile",
  SETTINGS = "/settings",
}
